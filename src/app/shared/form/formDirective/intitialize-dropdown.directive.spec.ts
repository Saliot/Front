/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IntitializeDropdownDirective } from './intitialize-dropdown.directive';

describe('Directive: IntitializeDropdown', () => {
  it('should create an instance', () => {
    let directive = new IntitializeDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
